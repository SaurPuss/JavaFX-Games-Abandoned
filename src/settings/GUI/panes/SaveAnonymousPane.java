package settings.GUI.panes;

import javafx.scene.control.TextField;
import javafx.scene.layout.VBox;
import settings.GUI.buttons.SignUpButton;

public class SaveAnonymousPane extends VBox implements SignUpButton {



    // TODO Make this a thing where it pulls the data and allows for save
    public SaveAnonymousPane() {
        TextField tName = new TextField();
        TextField tPassword = new TextField();





    }
}
